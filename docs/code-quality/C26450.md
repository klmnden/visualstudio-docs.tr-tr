---
title: C26450
keywords: C26450
author: mikeblome
ms.author: mblome
ms.date: 01/08/2017
ms.topic: reference
f1_keywords:
- C26450
helpviewer_keywords:
- C26450
dev_langs:
- C++
manager: jillfra
ms.openlocfilehash: 7971ebc4c21de15bfbcfd0e043208b276f33a3bf
ms.sourcegitcommit: 21d667104199c2493accec20c2388cf674b195c3
ms.translationtype: MT
ms.contentlocale: tr-TR
ms.lasthandoff: 02/08/2019
ms.locfileid: "55941343"
---
# <a name="arithmetic-overflow-operator-operation-causes-overflow-at-compile-time-use-a-wider-type-to-store-the-operands"></a>Aritmetik Taşma: '% işleci %' işlemi derleme zamanında taşmaya neden olur. İşlenenleri depolamak için daha geniş bir tür kullanın

 Bu uyarı, bir aritmetik işlem derleme zamanında kanıtlanabilir kayıplı olduğunu gösterir. İşlenen tüm derleme zamanı sabit olduğunda bu onaylanan.  Şu anda, sola kaydırma, çarpma, toplama ve çıkarma işlemlerinde böyle taşıyor denetleyin.

Not: C4307 Microsoft C++ derleyicisinde benzer bir denetimdir.

## <a name="example-1"></a>Örnek 1

```cpp
int multiply()
{
    const int a = INT_MAX;
    const int b = 2;
    int c = a * b; // C26450 reported here
    return c;
}
```
 Bu uyarıyı düzeltmek için aşağıdaki kodu kullanın.

```cpp
long long multiply()
{
    const int a = INT_MAX;
    const int b = 2;
    long long c = (long long)a * b; // OK
    return c;
}
```

## <a name="example-2"></a>Örnek 2

```cpp
int add()
{
    const int a = INT_MAX;
    const int b = 2;
    int c = a + b; // C26450 reported here
    return c;
}
```
 Bu uyarıyı düzeltmek için aşağıdaki kodu kullanın:

```cpp
long long add()
{
    const int a = INT_MAX;
    const int b = 2;
    long long c = (long long)a + b; // OK
    return c;
}
```
## <a name="example-3"></a>Örnek 3

```cpp
int subtract()
{
    const int a = -INT_MAX;
    const int b = 2;
    int c = a - b; // C26450 reported here
    return c;
}
```
 Bu uyarıyı düzeltmek için aşağıdaki kodu kullanın.

```cpp
long long subtract()
{
    const int a = -INT_MAX;
    const int b = 2;
    long long c = (long long)a - b; // OK
    return c;
}
```

## <a name="see-also"></a>Ayrıca Bkz.
[ES.103: Overflow yok](https://github.com/isocpp/CppCoreGuidelines/blob/master/CppCoreGuidelines.md#Res-overflow)
